import React from 'react';
import SearchBar from '../Components/SearchBar';

const Search = () => {
    return (
        <div>
            <h1>Search SPEED</h1>
            <SearchBar />
        </div>
    );
};

export default Search;